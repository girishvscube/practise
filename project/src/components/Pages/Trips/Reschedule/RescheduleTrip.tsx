import Schedule from '../ScheduleTrip/Schedule';
import BreadCrumb from '../../../common/Breadcrumb/BreadCrumb';

const RescheduleTrip = () => (
  <div>
    <BreadCrumb links={[
      
    ]}
    />
    <Schedule type='update' title="Reschedule Trip" />
  </div>
);

export default RescheduleTrip;
